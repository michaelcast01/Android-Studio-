package com.example.tiendasuplementacion.util

// AuditLogger removed per user request (no backend API available).
// This file intentionally provides a minimal no-op placeholder so
// the project remains compilable if any leftover references exist.

object AuditLogger {
    suspend fun log(event: Any?) {
        // intentionally no-op
    }
}
